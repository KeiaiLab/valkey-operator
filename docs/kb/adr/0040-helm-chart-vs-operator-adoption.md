# ADR-0040: Helm chart vs Operator 채택 정책 (외부 chart / 외부 chart / valkey-operator 비교)

- Date: 2026-05-10
- Status: Accepted
- Authors: @eightynine01
- Refs: ADR-0010 (cert-manager auto-discovery), ADR-0016 (ValkeyBackupTarget S3),
  ADR-0022 (minio-go v7), ADR-0024 (Helm Chart 수기 패턴),
  ADR-0035 (NetworkPolicy AutoCreate Optional)

## Context

2025-08 Broadcom 의 외부 chart 정책 변경으로 OSS Redis 배포 stack 의 기본 선택지가
재편됐다. 동일 시점에 본 valkey-operator 가 Cluster + Backup + cert-manager
통합 + PrometheusRule alert 자동 설치 등 핵심 운영 기능을 Accepted 상태로
보유하게 되어, "사내 또는 외부 사용자가 Redis/Valkey 를 어떻게 배포해야 하는가"
의 결정 기준 명시가 필요해졌다.

비교 대상:

1. **bitnami/redis** Helm chart (legacy / `bitnamilegacy` / `bitnamisecure` /
   유료 Secure Images 4-tier 분기)
2. **외부 chart-redis** Helm chart (2025 scratch 재구축, upstream image, OSS)
3. **valkey-operator** (본 repo, Operator + CRD)

추상화 레벨 차이: chart 2종은 *템플릿 엔진* — 사용자가 PVC/HA/backup/upgrade 를
직접 조립. operator 는 *동적 reconcile* — CR 1개로 모든 리소스 + lifecycle
자동화.

원외 분석 보고서 전문: `~/.claude/plans/1-https-artifacthub-io-packages-helm-clo-hazy-sketch.md`.

## Decision

다음 의사결정 매트릭스를 사내 표준으로 채택한다:

| 시나리오 | 권장 |
|---|---|
| 즉시 프로덕션, 작은 팀, 외부 의존 최소 | **외부 chart chart** |
| FIPS / CIS hardening 규제 의무 | **외부 chart Secure Images (유료)** |
| 사내 표준 + 동적 운영 자동화 (Cluster mode + auto-resharding + backup CRD) | **valkey-operator** |
| 외부 chart 기존 사용자 (2025-09 이후) | image registry override (`bitnamilegacy`) → 1~2 분기 내 마이그레이션 |

**valkey-operator 채택 조건** (5 gap 모두 해소 시 *Redis Enterprise 70~75% 수준*
도달 추정 — Cluster mode + 운영 자동화 시나리오에서 *unconditional* 권장 가능):

1. cert-manager **자체 CA 자동 생성** (`tls.autoGenerated` 동급, SelfSigned Issuer
   자동 생성 옵션) — 현재는 사용자가 외부 Issuer 명시 필수 (ADR-0010 의 후속).
2. **ValkeyBackupTarget GCS / Azure backend** — S3 단일 backend GA (ADR-0016) 의
   다중 클라우드 확장. 멀티 region DR 시나리오 대응.
3. **PVC auto-resize controller** — `Spec.Storage.Size` 변경 시 PVC `resources.requests.storage`
   patch + StatefulSet 재배포 자동화.
4. **Chaos engineering e2e** — kuttl / litmus 기반 random pod kill, network
   partition, disk fill 시나리오. 현재 `failover_test.go`, `cluster_recovery_test.go`
   는 *결정론적 시나리오* 만 커버.
5. **Troubleshooting + capacity planning 문서** — `docs/operations/runbook.md`
   는 release-checklist 중심. 운영자 친화적 문제해결 / 용량 산정 가이드 부재.

## Consequences

**긍정:**
- 외부 사용자 (OSS) 와 사내 운영자 모두 시나리오별 *권장 path* 가 명확해짐.
- "외부 chart paywall 회피 = 외부 chart" / "동적 자동화 = valkey-operator" 이중축
  으로 의사결정 시간 단축.
- 5 gap 이 명확화되어 GitHub epic + sub-issue 로 추적 가능 (별도 발급).

**부정:**
- valkey-operator 가 **3개 시나리오 중 1개** (Cluster + 자동화) 만 권장 — 본
  operator 의 *user base 확장 한계* 인정. 단순 standalone/replication 은
  외부 chart 가 더 가벼움.
- FIPS / CIS / Encryption at rest 는 5 gap 외 *별도 tier* (Pro 라인업 또는 사외
  의뢰) — 현재 로드맵에 없음 명시.

## Alternatives Considered

1. **외부 chart 무료판 유지** (image registry override 만): 2025-08 정책 후 보안
   패치 받지 못하는 `bitnamilegacy` 의존성 누적 → SEV-1 위험. 거절.

2. **외부 chart Secure Images 전사 도입** ($50K~$72K/년): FIPS 의무 환경에선
   합리. 그 외엔 비용 대비 가치 낮음. 일부 시나리오만 권장.

3. **외부 chart 단독 표준화**: Cluster mode auto-resharding 부재 → shard
   리밸런싱 매번 수동 `redis-cli --cluster reshard`. 운영 부담 큼. 거절 (단,
   simple 시나리오에선 권장).

4. **valkey-operator 전 시나리오 강제**: standalone 1개 인스턴스에 CRD + controller
   + webhook 설치는 over-engineering. 거절.

## Action Items

- [ ] AI-001: 본 ADR 의 5 gap 을 GitHub epic + 5 sub-issue 로 발급 (`epic`,
      `enhancement` 라벨)
- [ ] AI-002: README 의 "When to use" 섹션에 의사결정 매트릭스 4행 반영
- [ ] AI-003: docs/comparison/ 신규 디렉토리 + chart vs operator 상세 비교표
      (본 ADR 의 plan 파일 §2 기반 발췌)
- [ ] AI-004: 5 gap 모두 해소 후 *Status: Superseded by ADR-NNNN* 갱신 (조건부
      권장 → unconditional 권장 전환 시)

## References

- 외부 분석 보고서 (plan): `~/.claude/plans/1-https-artifacthub-io-packages-helm-clo-hazy-sketch.md`
- ADR-0010: cert-manager auto-discovery (Issuer 기반, *자체 CA 미포함*)
- ADR-0016: ValkeyBackupTarget S3 외부 저장 (GCS/Azure 미포함)
- ADR-0022: minio-go v7 채택 — S3 client 라이브러리
- ADR-0035: NetworkPolicy.AutoCreate Optional (default-deny 패턴 강점)
- 코드 검증: `internal/storage/s3_client.go` (141 LOC), `internal/controller/valkeybackuptarget_controller.go` (294 LOC), `internal/observability/`, `config/prometheus/alert-rules.yaml`, `charts/valkey-operator/templates/prometheusrule.yaml`
